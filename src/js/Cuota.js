class Cuota {
    constructor(number, month, value, state) {
        this.number = number;
        this.month = month;
        this.value = value;
        this.state = state;
    }
    setNumber(number) {
        this.number = number;
    }
    setMonth(month) {
        this.month = month;
    }
    setValue(value) {
        this.expire = expire;
    }

    setState(state) {
        this.state = state;
    }
    getNumber() {
        return this.number;
    }
    getMonth() {
        return this.month;
    }
    getValue() {
        return this.value;
    }

    getState() {
        return this.state;
    }
}